// Placeholder — full integration tests require a running Firebase emulator.
// Run the app on a device instead: flutter run
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
